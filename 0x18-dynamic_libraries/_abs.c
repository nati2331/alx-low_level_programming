#include "main.h"
/**
 * _abs - calculates the absolute value of an int.
 * @n: the int to check
 * Return: the absolute value of the int.
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
